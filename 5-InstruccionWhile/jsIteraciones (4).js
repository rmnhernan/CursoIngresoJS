function mostrar()
{
	var numeroIngresado;

	numeroIngresado = document.getElementById("Numero").value;

	var contador = 0

	var numero = prompt("ingrese un número entre 0 y 10.");

	while (numeroIngresado < 0 && numeroIngresado > 10 && isNaN ){

		alert ("Ingrese un numero entre 0 y 9");

	}

}//FIN DE LA FUNCIÓN